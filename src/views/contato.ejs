<%- include("./includes/head") %>
<%- include("./includes/nav") %>

<div class="container">
    <div class="row">
        <div class="col-lg-2"></div>
        <div class="col-lg-8 my-3">

          <h1 class="text-center">Contato</h1>
          <p class="text-center lead">teste aind</p>
            
          <%- include("./includes/menssages") %>

          <div class="row">
            <div class="col-lg">
              
                <% if(contato._id){ %>
                  <form class="form-contato" action="/contato/edit/<%= contato._id %>" method="post">
                <% }else{ %>
                  <form class="form-contato" action="/contato/register" method="post">
                <% } %>
                    <input type="hidden" name="_csrf" value = <%=csrfToken%> >
                    <div class="form-group">
                      <label for="nome">Nome</label>
                      <input value="<%= contato.nome %>" type="text" class="form-control" name="nome" placeholder="Digite um nome" autocomplete="off">
                    </div>
                    <div class="form-group">
                      <label for="sobrenome">Sobrenome</label>
                      <input value="<%= contato.sobrenome %>" type="text" class="form-control" name="sobrenome" autocomplete="off" placeholder="Digite uma sobrenome">
                    </div>
                    <div class="form-group">
                      <label for="email">Email</label>
                      <input value="<%= contato.email %>" type="email" class="form-control" name="email" placeholder="Digite um email" autocomplete="off">
                    </div>
                    <div class="form-group">
                      <label for="email">Telefone</label>
                      <input value="<%= contato.telefone %>" type="text" class="form-control" name="telefone" autocomplete="off" placeholder="Digite um número de telefone">
                    </div>
                    
                    <button type="submit" class="btn btn-primary my-3">Salvar</button>
                  </form>
            </div>
          </div>
        </div>
        <div class="col-lg-2"></div>
    </div>
</div>

<%- include("./includes/footer") %>